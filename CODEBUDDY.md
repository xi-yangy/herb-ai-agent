# CODEBUDDY.md — 项目宪章

> 本文件是「多模态中草药图像识别智能体」项目的**宪章与协作基线**，为 AI 助手与团队成员的日常开发提供统一的项目背景、技术决策、工作约定与合规红线。一切开发行为以本文档与 PRD 为准，冲突时以 PRD 为准并向用户确认。

---

## 1. 项目概述

### 一句话定位
一款面向大众消费者与中医药从业者的**多模态中草药图像识别智能体**，通过拍照/上传图片（可叠加文本、语音等模态输入）快速识别中草药，并提供功效、禁忌、安全提示与药典级专业信息。

### 核心价值主张
| 价值 | 说明 |
| --- | --- |
| **识别** | 识别鲜植物与常见中草药，快速给出识别结果 |
| **安全** | 面向大众突出用药安全警示与免责声明，降低误食/误用风险 |
| **专业** | 面向从业者提供真伪、品相鉴定与药典信息比对能力 |
| **多模态** | 图文 + 语音交互，降低使用门槛 |

### 项目背景
- 大学生暑期实训选题，产品形态为 Web/H5，移动优先 + 桌面端大屏适配（响应式）；
- 关键指标以实际验证结果为准，不虚标（识别准确率等）。

---

## 2. 目标用户

| 画像 | 人群 | 核心诉求 |
| --- | --- | --- |
| **画像一：中医药专业/从业者** | 中医师、执业药师、药材采购/验收/质检人员、院校师生 | 真伪鉴定、品相评估、药典信息比对；识别对象多为常见中草药 |
| **画像二：消费者** | 养生爱好者、户外人群、家庭照护者、普通患者家属 | 快速知道"这是什么药"、功效/用法/禁忌、明确的安全警示；识别对象多为鲜活植物 |

> 画像一为专业价值承载者，画像二为规模与合规重点。

---

## 3. 功能范围与优先级

### 3.1 功能矩阵（共 25 项，按 P0/P1/P2）
> 原始 P0/P1/P2 分级供远期迭代参考。**两周演示版（本次实训）实际按「必做/降级/砍掉」三档执行**，见下方 3.1.1。
- **P0（MVP 必备，11 项）**：拍照识别、相册上传识别、识别结果展示、易混淆高危辨析提示、功效主治/性味归经查询、用法用量/禁忌/毒性查询、用药安全提示与医疗免责声明、毒性/禁用高危警示、识别历史与收藏、注册登录、首次授权与隐私说明；
- **P1（迭代一，10 项）**：多模态图文+语音问答、语音输入、真伪鉴定、品相评估、药典比对、单味药材百科、拍摄部位引导、多图/批量识别、离线识别、结果分享；
- **P2（远期，4 项）**：社区/专家鉴定、鉴定报告导出 PDF、配伍/方剂查询、药材行情参考（另有国际化、开放 API、AR 识别、健康养生建议等延伸项）。

#### 3.1.1 两周演示版功能取舍（本次实训执行口径）
- **必做（核心闭环 + 创新点 + 多模态，14 项）**：拍照识别、相册上传识别、识别结果展示、**自训练本地模型识别（创新点，本实训实际完成）**、易混淆高危辨析提示、功效主治/性味归经查询、用法用量/禁忌/毒性查询、用药安全提示与医疗免责声明、毒性/禁用高危警示、**多模态图文+语音问答（P1 提前为必做）、语音输入（P1 提前为必做）**、首次授权与隐私说明；
- **降级（简化实现，3 项）**：识别历史与收藏（本地存储）、注册登录（本地存储，不接真实账户体系）、拍摄部位引导（首次简单引导文案）；
- **砍掉（口头提及/远期，其余）**：真伪鉴定、品相评估、药典比对、单味药材百科、多图/批量识别、离线识别、结果分享，及全部 P2 项。

### 3.2 功能边界（两周演示版）
- **包含**：识别主流程闭环（含**自训练本地模型主通道**）+ 安全合规底线 + 基本信息查询 + 历史记录（本地化）+ **多模态图文+语音问答**；
- **不包含（两周内不做，口头提及/远期）**：真伪/品相鉴定、药典比对、单味药材百科、多图批量识别、离线识别、结果分享、社区/专家鉴定、报告导出、行情、国际化、开放 API、AR、养生建议。

### 3.3 产品边界（红线）
- 任何识别/查询结果**不构成诊断或处方建议**；
- 不提供药品交易/导购功能；
- 不生成健康养生个性化建议（P2 项需谨慎评估后启动）。

---

## 4. 技术方案（已确定）

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 前端 | Vue 3 + Vant + Tailwind CSS | H5 响应式：移动优先（≤375px）/ 平板（768px）/ 桌面（≥1024px，分栏布局，最大阅读宽度约 1200px） |
| 前端增强 | PWA | 可安装、离线缓存（配合 P1 离线识别） |
| 多模态交互 | 通义千问（Qwen）API | 图文/语音问答（**两周必做**，失败回退知识库结构化展示） |
| 语音输入 | Web Speech API | 语音转文本（**两周必做**，需联网 + Chrome 系浏览器） |
| 后端 | Python FastAPI | 识别代理、知识库查询、历史记录接口；提供类型标注 |
| 识别引擎 | **自训练本地模型（PyTorch ResNet50）+ 百度植物识别兜底** | 用 `training/train.py` 自训练药材分类模型作本地主通道（`LocalRecognizer` 加载，channel=local）；低置信/未启用/异常时回退百度（channel=baidu），再兜底 Mock（channel=mock），保证链路不中断 |
| 数据存储 | SQLite（起步）→ MySQL | 表：users、herbs、recognition_history、favorites、privacy_consents |
| 部署 | 方案一：本地 + 内网穿透；方案二：云服务器 | 演示以方案一为主，长期展示可迁移方案二 |

### 4.1 核心接口（概要）
- `POST /api/recognize`：图片识别（返回通道 local/baidu/mock、置信度、安全等级、相似品种、分层信息）
- `GET /api/herbs/{id}`：药材知识库详情
- `GET /api/search/herbs`：百科搜索
- `POST /api/qa`（**必做**）：多模态问答（识别结果上下文 + 文本/语音，Qwen 真实调用 + 降级兜底）
- `POST /api/auth/*`：注册/登录
- `GET/POST/DELETE /api/history`、`/api/favorites`：历史与收藏

### 4.2 关键设计决策
- **置信度阈值**：低于阈值不直接给结论，降级为"相似品种列表"并引导重拍；
- **安全等级字段**：知识库每条药材必填安全等级（普通/慎用/毒性），联动 UI 警示；
- **高危判定宁严勿松**：可疑即触发强制全屏警示确认页（无跳过入口）；
- **性能**：识别接口 ≤3s（含网络）、图片前端压缩 ≤1MB、结果页首屏 ≤2s。

---

## 5. 里程碑与排期（2 周实训周期）

| 阶段 | 时间 | 目标 | 交付物 |
| --- | --- | --- | --- |
| 1. 准备与识别闭环 | 第 1 周前半（约 3 天） | 环境搭建、百度/Qwen API 验证、**采集数据集并训练自训练本地模型**、打通识别主链路 | 图片→识别→结果最小 demo（本地模型主通道） |
| 2. 核心开发 | 第 1 周后半–第 2 周前半（约 5 天） | 两周必做功能（识别 + 多模态问答 + 合规底线） | 两周演示版可运行版本 |
| 3. 多模态交互 + 演示打磨 | 第 2 周后半（约 2 天） | 结果页追问 + 语音输入 + 桌面投屏适配 + 联调演练 | 可演示部署 + 演示脚本 |

---

## 6. 合规底线（全流程强制）

1. **医疗免责声明**：所有结果页底部固定显示——"结果仅供参考，不构成诊断或处方，如有不适请咨询执业医师/药师"；任何页面不出现诊断/处方性表述；
2. **高危强制警示**：金银花 vs 断肠草、乌头类等高危/易混淆品种识别结果触发强制全屏警示确认页，不可跳过、无关闭入口；
3. **隐私与图片数据**：用户上传图片仅用于本次识别，前端完成后即清除，服务端不持久化原始图片（或明确保留期限并告知）；首次使用弹窗说明相机/相册/麦克风权限用途；拒绝授权时功能降级提示而非强制阻断；
4. **数据合规**：《中国药典》等内容仅引用与摘要并标注来源，不整本复制；数据集遵守许可协议；用户上传图片不得擅自用于模型训练；
5. **内容审核**：知识库高风险字段（用法用量、毒性）上线前需人工/专家复核并留痕。

---

## 7. 质量与验收基线

### 验收对象
**两周演示版功能集**（必做核心闭环 + 多模态问答 + 合规底线）。验收标准详见 PRD 第 3 章（F1–F13 功能验收、Q1–Q4 质量标准、U1–U4 用户验收）。

### 质量要求（开发时必须遵守）
- **Q1 代码质量**：前后端统一风格规范，后端类型标注，关键模块注释；无未处理阻塞性异常；
- **Q2 测试覆盖**：后端核心链路（识别路由、知识库查询、历史记录）有自动化冒烟测试；高危警示判定逻辑有单元测试；
- **Q3 性能**：识别 P95 ≤3s、图片 ≤1MB、首屏 ≤2s、百并发压测无崩溃；
- **Q4 安全审查**：免责声明与隐私说明全流程覆盖；用户图片不持久化；高危警示无跳过入口；接口鉴权与限流生效。

### 演示目标
- 识别主流程"打开 → 拍照 → 出结果"≤10 秒；
- **多模态闭环演示**："识别 → 结果页追问（文本/语音）→ Qwen 生成回答 + 免责声明"流畅呈现；
- 桌面端 ≥1024px 结果页分栏布局（左图对比 / 右信息卡 + 警示区），投屏可读；
- 本地 + 内网穿透链路可稳定演示 ≥30 分钟。

---

## 8. 风险与待核实项

| 类型 | 风险 | 缓解策略 |
| --- | --- | --- |
| 技术 | 百度植物识别对部分药材识别准确率不足 | **自训练本地模型主通道提升准确率**；低置信度时降级为相似品种列表并引导重拍；高危判定宁严勿松兜底 |
| 技术 | 自训练模型准确率有限 / 过拟合（实测验证约 77.8%） | 本地低置信自动回退百度兜底；易混淆药材靠高危辨析机制补强；后续扩充数据/调参/换骨干提升 |
| 技术 | Qwen 多模态能力与免费额度不确定 | 多模态为两周必做，第 1 周即验证 vision；问答不可用降级为知识库展示，不阻塞演示 |
| 依赖 | 百度植物识别 API 计费与限制变动 | 免费额度起步，监控成本；本地模型为主通道降低 API 依赖；识别失败回退 Mock 演示数据 |
| 进度 | 实训周期压缩为两周 | 聚焦「识别闭环 + 多模态问答 + 合规底线」一条主线，专业鉴定/百科等远期项暂缓 |

---

## 9. 协作与文档规范

### 9.1 工作约定
- **文档驱动**：需求/设计变更先更新文档，再进入开发；PRD 为开发唯一执行依据，不自行新增功能；
- **文件命名**：PRD 遵循 `{feature_name}-v{version}-prd.md` 规范，存于 `docs/prds/`；
- **不虚标**：识别范围、准确率等以实际验证结果如实标注，不引用厂商自述作为承诺；
- **提交规范**：提交信息简洁说明变更意图；不提交生成物/临时文件（如训练缓存、内网穿透日志）。

### 9.2 目录结构（规划）
```
d:/CodeBuddy/herb-ai-agent/
├── CODEBUDDY.md                    # 项目宪章（本文件）
├── docs/
│   ├── 多模态中草药图像识别智能体-初版需求文档.md   # 需求文档 v0.4（定稿评审稿）
│   └── prds/
│       └── 多模态中草药图像识别智能体-v1.0-prd.md  # PRD v1.0（开发执行依据）
├── frontend/                       # [规划] Vue 3 + Vant + Tailwind
├── backend/                        # [规划] Python FastAPI
├── training/                       # [规划] 自训练模型（PyTorch ResNet50 训练脚本，产物入后端识别主通道）
└── data/                           # [规划] 药材知识库数据
```

### 9.3 参考文档
| 文档 | 路径 | 说明 |
| --- | --- | --- |
| PRD v1.0 | `docs/prds/多模态中草药图像识别智能体-v1.0-prd.md` | 开发执行依据（含验收标准、风险、排期） |
| 需求文档 v0.4 | `docs/多模态中草药图像识别智能体-初版需求文档.md` | PRD 来源，含竞品分析、功能矩阵、数据合规 |

---

## 10. AI 助手工作准则（本文件面向 CodeBuddy）

1. **以 PRD 为唯一依据**：不实现 PRD 功能边界之外的诉求；除两周必做项（含多模态问答）外，其余 P1/P2 功能未经用户确认不提前实现；
2. **合规优先**：任何代码/文案不得出现诊断/处方性表述；高危警示逻辑（不可跳过）为硬性要求，不得弱化；
3. **先读文档再动手**：开发前先阅读 PRD 相关章节与现有代码结构，避免重复造轮子；
4. **保持轻量**：不创建临时脚本遗留工作区，不引入 PRD 未约定的依赖；
5. **诚实反馈**：识别准确率、API 额度等未验证事项如实标注，不臆测。

---

**文档版本**：1.2（两周演示版）
**创建时间**：2026-08-27
**更新时间**：2026-08-29
**依据文档**：《多模态中草药图像识别智能体-v1.0-prd.md》（PRD v1.2）、《多模态中草药图像识别智能体-初版需求文档.md》（v0.6）

**版本记录**：1.1 → 1.2 补回**自训练本地模型识别**创新点（PyTorch ResNet50 训练 + 本地主通道 + 百度兜底 + Mock 保底），更新技术方案、功能边界、风险、里程碑与目录结构。
