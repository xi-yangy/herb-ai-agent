# 终验 PPT 截图清单

> 配合《终验PPT内容稿-v1.0.md》（v1.1）使用：按优先级勾选截图，先截必截项，再截推荐项与加分项。
> 截图建议以编号命名（如 `01a-结果页首屏.png`、`01b-分层信息卡.png`、`02-警示页.png`），套模板时便于对应。
>
> - **版本**：v1.1
> - **创建时间**：2026-09-02

---

## 一、必截项（PPT 核心配图，先做这 3 张）

- [ ] **01a-结果页首屏（普通药材）**：首页相册上传「黄芪」→ 识别后停在首屏截图，需露出：绿色安全标签、置信度、识别通道 → 放 **P7**
- [ ] **01b-分层信息卡**：结果页向下滚动到分层信息卡位置截图（白话科普默认展开态；可选另截一张展开「专业药典」的）→ 放 **P7**
- [ ] **02-高危全屏警示页（毒性药材）**：上传「附子」→ 自动跳转全屏警示 → 整页截图，需露出：单按钮「我已了解风险」、无关闭 / X / 跳过 → 放 **P7**

## 二、推荐项（增强说服力，有时间就截）

- [ ] **03-首页**：打开 `http://localhost:5173` 整页：品牌区「灵草」+ 三大亮点标签 + 安全三原则 → 放 P1 封面 / P2
- [ ] **04-问答区**：结果页向下滚动到 `QaPanel`：聊天气泡 + 语音按钮 + 底部免责声明 → 放 P8
- [ ] **05-隐私说明弹窗**：清掉 localStorage 的 `herb_privacy_ack` 后刷新首页（DevTools → Application → Local Storage → 删除）→ 截弹窗 → 放 P10 / P6
- [ ] **06-历史 / 收藏 / 我的**：底部 Tab「历史」按时间分组列表；「我的」权限管理 → 放 P6（一句话带过）

## 三、加分项（AI 协作 20 分的"证据图"，强烈建议截）

- [ ] **07a-GitHub Commits 记录（推荐）**：访问 `https://github.com/xi-yangy/herb-ai-agent/commits/main` 整页截图，展示按时间倒序的提交历史（提交标题 / 作者 / 时间）→ 放 **P10 右栏「有效迭代轮次」**；GitHub 无法访问时改用命令行 `git --no-pager log --pretty=format:"%h %ad %s" --date=format:"%m-%d" -20`（终端拉宽、深色背景）截图
- [ ] **07b-仓库主页（可选加分）**：`https://github.com/xi-yangy/herb-ai-agent` 的 Code 标签页，展示目录结构与最新提交动态 → 放 **P10**
- [ ] **08-训练日志（可选）**：后端日志「本地模型已加载（93 类，设备 cuda/cpu）」那一段 → 放 P5 算法页
- [ ] **09-纠错证据（可选）**：若还留着 `.venv` 报错日志或乱码目录脚本片段，可截图作为「发现并纠正 AI 错误」佐证 → 放 P10 左栏

---

## 四、操作步骤（一次跑通）

1. 后端：系统 Python 后台启动（`python run.py`），确认日志出现「本地模型已加载（93 类，设备 cuda/cpu）」；
2. 前端：`cd d:\CodeBuddy\herb-ai-agent\frontend` 后 `npm run dev:raw`；
3. Chrome 打开 `http://localhost:5173`，建议用**桌面端窗口（1280×800，投屏形态）**；
4. 按上面顺序逐项操作 + 截图（`Win+Shift+S` 区域截图即可）；
5. 截图以编号命名保存，便于套模板时对应。

> 提示：截完一张勾选一项；必截 01a/01b/02 全部在前端页面操作完成，无需改任何代码。

---

## 版本记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| 1.0 | 2026-09-02 | 初版：按终验 PPT 内容稿 v1.1 编排的截图清单（必截 / 推荐 / 加分 + 操作步骤） |
| 1.1 | 2026-09-02 | ① 01 拆分为 01a 首屏 / 01b 分层卡（结果页一屏截不全）；② 07 改为 07a GitHub Commits 记录（含命令行兜底命令）/ 07b 仓库主页；③ 更新命名示例 |
