# 多模态中草药图像识别智能体 - 终验 PPT 内容稿

> 本文档为**项目终验 PPT** 的逐页内容稿，供本人套模板排版使用。每页含【页面要点】（排版 bullet）与【讲解词】（可直接照念）。
> 所有数字与表述均为**实测口径**（识别准确率 77.8% 等如实标注），不虚标；合规红线（免责声明、高危警示、不虚标）全程保留。
>
> - **版本**：v1.0
> - **创建时间**：2026-09-02
> - **依据文档**：PRD v1.2、初验演示脚本 v1.0、结项演示脚本 v1.0、项目宪章 CODEBUDDY.md v1.4
> - **评分对齐**：功能 10 + 界面 10 + 创新 10 + AI 协作 20 + 演示 10（AI 协作单项最重，P10 单独一页）
> - **适用时长**：主稿按 10 分钟（12 页）撰写；第 13 节给出 5 分钟压缩策略。

---

## 0. 总览：模块 → 页面 → 分值映射

| 老师要求模块 | 对应页面 | 关联分值 |
| --- | --- | --- |
| 设计思路 | P2 | 功能 / 创新 |
| 过程 | P3 | AI协作（协作过程） |
| 技术 | P4 | 功能 |
| 算法 | P5 | 创新 |
| 功能 | P6 | 功能 10 |
| 界面 | P7 | 界面 10 |
| 创新点 | P8 | 创新 10 |
| 难点 | P9 | AI协作（批判性思维） |
| **AI协作（专项页）** | **P10** | **20 分（单项最重）** |
| 总结和展望 | P11 | — |
| 演示 | P6/P7 衔接现场 | 演示 10 |

---

## P1 封面

- **标题**：多模态中草药图像识别智能体
- **副标题**：拍照识别 · 用药安全 · 多模态问答
- 占位：团队 / 成员 / 日期

**讲解词**：
> 各位老师好，我们组的项目是《多模态中草药图像识别智能体》。一句话概括：用户拍照或上传一张中草药图片，系统识别出是什么药，同时给出用药安全提示，还能用文字或语音追问。接下来我从设计思路、技术算法到创新难点，完整介绍一下这个项目。

---

## P2 设计思路

- 痛点：大众对不认识的草药易**误采误食**；网络信息鱼龙混杂；市面识花 App 只认鲜植物、**不含用药安全提示**；
- 定位：把「识别」和「安全」绑定，识别即给安全分级（普通 / 慎用 / 毒性）；
- 双画像：大众消费者（要"怎么安全用"）+ 中医药从业者（要专业药典信息）；
- 差异化：不做识花 App，做**有安全底线的中药识别**。

**讲解词**：
> 设计思路从痛点出发：大众遇到不认识的草药容易误采误食，网上的信息又难辨真伪，而市面识花 App 只能认植物，回答不了"有毒没毒、怎么用"。所以我们把"识别"和"安全"绑定，识别结果直接给安全等级和警示；同时服务两类人——普通大众看白话科普，从业者看专业药典信息。

---

## P3 项目过程

- 文档驱动：需求 v0.4→v0.6、PRD v1.0→v1.2、项目宪章 v1.1→v1.4，先定文档再开发；
- 里程碑：识别闭环 → 安全合规 → 多模态问答 → 演示打磨；
- 人机协作迭代：每个功能先出方案 → 人工确认 → 实现 → 验证 → 同步仓库，逐模块推进。

**讲解词**：
> 项目过程采用文档驱动：先把需求、PRD、验收标准定下来，再进入开发。我们按四个里程碑推进——识别闭环、安全合规、多模态问答、演示打磨；整个过程是和 AI 协作完成的多轮迭代，文档版本从 v0.4 一路迭代到 v1.2，每个模块完成即同步仓库，保证可演示版本始终可用。

---

## P4 技术架构

- 前端：Vue 3 + Vant + Tailwind（H5 响应式，移动优先 / 桌面 ≥1024px 分栏）；
- 后端：Python FastAPI（识别代理、知识库、问答、历史）；
- 识别引擎：**自训练本地模型（主）→ 百度植物识别（兜底）→ Mock（保底）**；
- 多模态：**通义千问 Qwen（主）→ 知识库结构化（降级）**；
- 数据：SQLite，172 味药材知识库（功效 / 禁忌 / 毒性 / 安全等级）。

**讲解词**：
> 技术上是前后端分离：前端 Vue3 + Vant，移动优先也适配桌面大屏；后端 FastAPI。识别引擎是三通道设计——自训练本地模型是主通道，低置信度自动回退百度，再不可用就用模拟数据保底，链路永远不断。多模态问答用通义千问，失败降级本地知识库。数据层是 SQLite，内置 172 味药材的结构化知识库。

---

## P5 核心算法

- **自训练分类模型**：自建数据集 → ResNet50 迁移学习 → 识别主通道，覆盖 **93 类**常用药材，验证集实测准确率约 **77.8%**（如实标注）；
- **三通道调度**：local 置信度 < 0.6 → 回退 baidu → 异常兜底 mock，页面标注实际通道；
- **低置信策略**：不给直接结论，展示相似品种列表 + 引导重拍，避免误导；
- **问答降级**：Qwen 超时 / 未配置 → 知识库结构化摘要 + fallback 标记。

**讲解词**：
> 核心算法是我们自训练的 ResNet50 分类模型：整理数据集、迁移学习、部署成主通道，覆盖 93 类常用药材，验证集实测准确率约 77.8%，如实标注。调度上，本地置信度低于 0.6 自动回退百度，异常再兜底 mock；低置信时不给直接结论，而是展示相似品种引导重拍，避免误导。问答侧 Qwen 不可用就降级为知识库结构化展示，链路不中断。

---

## P6 功能清单

- **识别**：拍照 / 相册上传，≤3s 出结果，图片压缩 ≤1MB；
- **安全**：三级安全色标、毒性强制全屏警示（不可跳过）、易混淆防雷警报卡；
- **信息**：白话科普 + 专业药典**分层信息卡**；
- **多模态**：结果页文本 / 语音追问（Web Speech API）、回答附免责声明；
- **历史 / 收藏 / 我的**：本地化记录、权限管理；
- **首次授权**：隐私说明弹窗，可拒绝并降级。

**讲解词**：
> 功能上聚焦一条主线：识别、安全、信息、多模态。识别支持拍照和相册上传，几秒出结果；安全上有三级色标和毒性强制警示；信息是白话加药典的分层卡；多模态支持语音追问；另有历史收藏和首次授权说明等配套功能。

---

## P7 界面设计

- 移动优先 H5，桌面 ≥1024px **分栏布局**（左图对比 / 右信息卡 + 警示区），投屏可读；
- 安全等级**三色体系**：普通=绿、慎用=橙、毒性=红，一屏即知；
- 全屏警示页**无关闭 / 跳过入口**，只有"我已了解风险"；
- 分层信息卡：白话科普（限定词表述）+ 专业药典双视角；
- 品牌视觉：「灵草」绿色系，统一组件（Vant + Tailwind）。

**讲解词**：
> 界面设计围绕安全和可读性：移动端单手可操作，桌面端分栏展示，结果一屏呈现安全色标；毒性药材触发全屏警示，没有关闭和跳过入口，视觉上强制；信息卡分白话和专业两档，照顾两类用户；整体用统一的绿色系品牌视觉。

---

## P8 创新点

1. **自训练本地模型作主通道**：走通"数据采集 → 训练 → 部署 → 识别"完整工程闭环，摆脱对单一第三方 API 的依赖；
2. **安全合规闭环**：高危强制警示不可跳过 + 全流程免责声明 + 防雷警报卡，把"安全"从口号做成硬机制；
3. **多模态交互**：图文 + 语音问答，Qwen 真实调用 + 降级兜底，演示链路不断。

**讲解词**：
> 创新点有三个。一是自训练本地模型做主通道，整个数据到识别的闭环自己做通，减少第三方依赖；二是安全合规闭环，高危警示不可跳过、免责声明全覆盖，这是同主题项目里少见的硬机制；三是多模态交互，文本加语音都能问，且带容错降级。

---

## P9 难点与解决

| 难点 | 分析与解决 |
| --- | --- |
| 单模型准确率有限（实测 77.8%） | 承认局限，低置信回退百度 + 相似列表引导，不误导用户 |
| 第三方 API 依赖 / 可用性不确定 | 本地模型主通道 + Qwen 降级知识库，链路永远不断 |
| 语音 / 识别环境受限 | Web Speech API 需 Chrome + 联网，失败提示降级文本输入 |
| 数据合规 | 图片不持久化、权限说明、高风险字段人工复核、不虚标 |

**讲解词**：
> 项目最大的难点是识别准确率和外部依赖的不确定性。我们如实标注准确率 77.8%，并用三通道容错和低置信引导来弥补；对 Qwen、语音这些外部能力，全部做了边界分析——边界内使用、边界外降级，不阻塞主流程。

---

## P10 AI 协作专项页 ★（20 分，单独一页）

**标题**：AI 协作实践：批判性思维 × 人机协作流程

**左栏｜批判性思维（10 分）**
- 发现并纠正 AI 错误：
  - 实测发现「用 .venv 启动 → torch 缺失 → 识别静默回退百度」，定位根因并纠正为系统 Python；
  - 否决 AI 初期"全依赖第三方 API"方案，改自训练本地模型主通道；
  - 解决 git 中文提交乱码（GBK），改 UTF-8 提交文件方式。
- 多角度验证：准确率不用厂商自述，自建验证集实测 77.8%；三通道逐一冒烟；高危警示有单元测试。
- 能力边界分析：77.8% 局限 → 回退兜底；Qwen / 语音边界外降级；百度覆盖物种不虚标。

**右栏｜协作过程（10 分）**
- 人机协作流程：文档驱动 + 先规划后执行——需求 → PRD → 方案 → 人工确认 → 实现 → 验证 → 同步；
- 有效迭代轮次：需求 v0.4→v0.6、PRD v1.0→v1.2、宪章 v1.1→v1.4；识别链路多轮调优；
- 人机分工标注：**人**管需求、决策、合规验收、演示；**AI** 管代码实现、训练脚本、文档、排障；合规红线由人最终把关。

**讲解词**（60–75 秒）：
> AI 协作贯穿项目全程。批判性思维上，我们多次发现并纠正 AI 的错误或偏差：比如实测发现用 .venv 启动后端会导致 torch 缺失、识别静默回退百度，我们定位根因纠正了环境方案；也否决过 AI"全依赖第三方 API"的倾向，改为自训练本地模型。验证上，准确率用自建验证集实测、不采信厂商自述；三通道和降级路径都逐一冒烟。能力边界上，我们明确承认单模型局限，用回退兜底，外部能力边界外一律降级。
> 协作过程上，我们采用文档驱动、先规划后执行：每个功能先出方案、人确认再动手，需求文档从 v0.4 迭代到 v0.6、PRD 从 v1.0 到 v1.2。分工上，人负责需求决策、合规验收和演示，AI 负责代码、训练脚本、文档和排障；合规红线这类硬要求，由人最终把关。

---

## P11 总结与展望

- **总结**：聚焦"识别 + 安全 + 多模态问答"核心闭环，做深做稳；三通道容错保证链路稳定；合规红线全程落地；
- **展望**：扩充数据集重训扩类，逐步逼近药典常用药材覆盖；预留真伪鉴定、药典比对、多图批量、离线识别等扩展点；架构可长期演进。

**讲解词**：
> 总结下来，我们有意把功能收敛在识别、安全、问答这条主线上，把核心链路做深做稳，而不是铺开一堆浅尝辄止的功能。展望上，模型可以靠补数据重训持续扩类，架构上预留了真伪鉴定、药典比对、多图批量等扩展点，是一个可长期演进的基础。

---

## P12 结束页

- **谢谢聆听 · 欢迎各位老师批评指正**

**讲解词**：
> 我的介绍就到这里，感谢各位老师，欢迎批评指正。

---

## 13. 5 分钟压缩版策略

- **保留**：P1、P5、P6（口头快过）、P7（一句话 + 截图）、P8、**P10（必留）**、P12；
- **合并**：P2 + P3 合并讲 30 秒；P9 并入 P10 左栏一句话；P11 压成结尾两句；
- 现场演示穿插在 P6 / P7 之后约 1 分钟。

---

## 14. 版本记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| 1.0 | 2026-09-02 | 初版：按评分分值（AI协作20分专项页）+ 老师指定模块（设计思路/过程/技术/算法/功能/界面/创新点/难点/总结展望）编写 12 页内容稿，附 5 分钟压缩策略 |
